project: customer-support-langchain
agents:
  triage:
    system_prompt: |
      You are the first-pass support triage agent. Classify customer issues by urgency, product area, and account impact before routing to a specialist chain.
  retrieval:
    instructions: |
      Prefer grounded answers from the internal knowledge base. Cite the source document id when returning a final support answer.

